export const CAPTURE_CARDS_TARGET = {
  player: { x: window.innerWidth / 2, y: window.innerHeight },
  opponent: { x: window.innerWidth / 2, y: -window.innerHeight },
};
export const PLAY_CARD_TARGET = {
  player: { x: window.innerWidth / 2, y: window.innerHeight / 2 },
  opponent: { x: window.innerWidth / 2, y: -window.innerHeight / 2 },
};
